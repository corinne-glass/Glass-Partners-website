# Glass Partners — copy reference

Canonical strings live in `content/site.ts`. Homepage and `/about` follow the latest refresh (tension-first hero, tightened sections, selective CTA).

## Positioning (footer / meta / sr-only on hero)

We fix the gap between what your business knows and what it actually delivers.

## Testimonials

Add quotes in `content/site.ts` → `testimonials[].quote`. Empty quotes show a dashed placeholder; filled quotes render under “What clients say”.

## Default contact

`corinne@corinneglass.com` — override with `NEXT_PUBLIC_CONTACT_EMAIL` in `.env.local`.
